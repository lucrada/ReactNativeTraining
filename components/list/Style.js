import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    list: {
        padding: 10,
        backgroundColor: "teal",
        marginBottom: 10,
        borderRadius: 10,
    },
    list_title: {
        color: "#fff",
        fontSize: 20,
    },
    list_content: {
        color: "#fff",
        fontSize: 16,
    }
});